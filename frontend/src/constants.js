export const START_GAME_URL = 
  `https://localhost:8090/api/startGame/`;

//   export const PATCH_MOVE_TICKET_URL = (ticketId, state) =>
//   `https://localhost:8090/api/startGame/Move?ticketId=${ticketId}&state=${state}`;